class Solution:      #removes all integers of value, val from the list nums 
    def removeElement(self, nums: List[int], val: int) -> int:
        
        i=0
        while i <len(nums):
            print(i)
            if nums[i]==val:
                nums.remove(val)
            else:
                i+=1
